/**
 * Created by tania on 28.5.2017 г..
 */
function triangleOfDollars(n) {
    let line = '';
    for(let i = 1 ; i<=n; i++){
        line += '$';
        console.log(line);
    }
}

//triangleOfDollars(3);